In this task used all os.methods() of pythons that specified in condition of task.

Before run code need to create just  2 empty folders named Source and Replica on the desktop.

After run the code in inside of folders will created the same file with wrote some words.